# ── Frontend Environment Variables (Vercel) ──
# Backend API URL (optional). Prefer relative /api to avoid mixed-content.
# Local with Vite proxy: /api
# Production with reverse proxy: /api
VITE_API_URL=/api
